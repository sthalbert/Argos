{{- if .Values.podDisruptionBudget.enabled -}}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "argos-ingest-gw.fullname" . }}
  labels:
    {{- include "argos-ingest-gw.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "argos-ingest-gw.selectorLabels" . | nindent 6 }}
{{- end }}
